function hello(repeat: number, to: string): void {
  let text: string = "hello"
  let result: string = "";
  let index: number = 1;
  for (let i =)
  if (repeat === 0) {
      console.log(result + "!");
    } else {
      console.log(result + to + "!");
    }
}

hello(3, "a")