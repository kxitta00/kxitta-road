function printNumbers(nums) {
  //nums =  [10,10,10]
  //         1  2  3 length
  //         0  1  2 index


  let index = 0;
  const num_length = nums.length //

  while (index < num_length) {
    const num = nums[index]
    if (num !== undefined) {
      const numString = (num)
    }
  }

}

printNumbers([10, 10, 10])
